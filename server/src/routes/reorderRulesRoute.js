const express = require('express');
const router = express.Router();

const reorderRulesController = require('../controllers/reorderRulesController')

// Getting all
router.get('/reorderRules', );

// Getting one
router.get('/reorderRules/:id', 
)

// Creating one
router.post('/reorderRules/', )
 
 // Updating one
router.patch('/reorderRules/:id',

)
// Delete one
router.delete('/reorderRules/:id',
 
)

module.exports = router;